# 「秘书」自身_日志（秘书自己的执行记录，只追加）

> 用法：秘书自身（方法层/结构/部署）的每次改动，按执行协议记一条。秘书自身的"锚点卡" = `method/00_秘书宪法.md` 第二~四节；"本质三问" = 宪法第三节秘书自身三问。
> 升级规则：**同类偏差 ≥3 次 → 修改方法层（走提案）**，而不是继续靠自觉。
> 去名化约定（2026-08-30 起）：日志不出现任何具体接入项目的名称，一律用"首个接入项目 / 该项目"指代——保证主库零项目污染。

## 模板

```
- [日期] [类型: 结构/方法/部署/偏差] 说明
  - 开工行为句：用户做了什么 → 得到什么 → 因此更可能发生什么
  - 锚点：秘书核心命题的哪个动作（接入 / 锚定 / 纠偏）
  - 三问预检：①… ②… ③…
  - 执行摘要：（行为句）
  - 完工核对：✅ 一致 / ❌ 有偏差
  - 下一步锚点：xxx
```

## 记录区

- [2026-08-30] [结构] 秘书独立化 + 克隆接入首个项目（创始人指令：防项目混淆、隔离、实测效果）
  - 开工行为句：创始人把秘书移出原项目目录 → 得到"独立主库 + 专服该项目的克隆副本"两个互不污染的项目 → 因此接入任何项目时不再与具体项目混淆，方法层改动与项目数据各自隔离
  - 锚点：秘书核心命题的"接入"动作 + 隔离纪律
  - 三问预检：① 主库/副本每个文件真实可用 ✅ ② 让首个接入项目的下一步（走副本协议）更接近本质 ✅ ③ 本记录行为句可描述 ✅
  - 执行摘要：主库移出原项目目录独立成项目（git init）；克隆副本专服该项目（持有其实例数据）；主库移除项目实例数据、登记表改为纯模板；两侧 README 写入隔离规则（主库不碰项目数据、副本不碰方法层）；主库新增本文件作为秘书自身执行记录。
  - 完工核对：✅ 一致
  - 下一步锚点：项目执行全部在克隆副本内按协议走（开工读副本内实例锚点卡）

- [2026-08-30] [偏差-工程] init.sh 变量名解析 bug（bash 3.2 下 `$DIR` 紧邻全角括号「（」时，变量名解析粘连多字节字符）
  - 现象（行为句）：执行 `bash init.sh demo` 时第 26 行报 `DIR�: unbound variable`，脚本中断
  - 原因：`$DIR` 与全角括号相邻，旧版 bash 解析变量名时把多字节字符并入变量名
  - 回锚动作：改为 `${DIR}` 显式定界；重测通过（demo 实例生成、登记追加、防覆盖保护均 ✅）
  - 结果：✅ 接入脚本可用；教训沉淀——中文语境脚本一律用 `${VAR}` 定界

- [2026-08-30] [部署] Git 归档并推送独立主库至 git@github.com:roxanna223/mishu.git（创始人指令）
  - 开工行为句：创始人把独立秘书主库 git 归档并推送到远程 → 得到可迁移、防丢失、可复用的工程主库 → 因此主库不再只存在于本机目录，版本历史与隔离状态可追溯
  - 锚点：秘书核心命题的"接入"动作（任何机器可克隆主库、克隆副本服务项目）
  - 三问预检：① 主库文件真实可用 ✅ ② 让首个接入项目的下一步更接近本质 ✅ ③ 行为句可描述 ✅
  - 执行摘要：全量文件入 git、首次提交、添加远程 origin、主分支改名 main、打 tag v2026.08.30-mishu-main、推送（含 .gitignore 防 .DS_Store）
  - 完工核对：✅ 一致
  - 下一步锚点：项目执行仍在克隆副本内按协议走

- [2026-08-30] [修正] 主库去项目名化（创始人指令：独立主库必须零项目污染）
  - 开工行为句：创始人发现主库混有具体项目痕迹 → 要求全部去除 → 因此主库对任何项目保持中立，克隆复用时不带任何第三方内容
  - 锚点：秘书核心命题的"接入"动作（主库的独立性是复用的前提）
  - 三问预检：① 修改后主库文件真实可用 ✅ ② 让复用者拿到纯净方法层 ✅ ③ 行为句可描述 ✅
  - 执行摘要：方法层示例去名化（改为中性示例）；README 示例路径改为 `<项目目录>/秘书`；projects/README.md 清空登记行、改为纯模板；本日志既有条目去名化改写（事实不变）并新增本条；修正根提交（amend）、重建 tag、强制推送。
  - 完工核对：✅ 一致（全库 grep 无项目名残留，见本轮验证）
  - 下一步锚点：项目执行仍在克隆副本内按协议走
